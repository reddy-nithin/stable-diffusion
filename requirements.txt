# ── COLAB INSTALL STRATEGY ───────────────────────────────────────────────────
# Colab pre-installs: torch, torchvision, numpy, scipy, pandas, transformers,
# accelerate, peft. DO NOT reinstall those — it breaks CUDA version matching.
#
# Only install packages Colab does NOT ship:
# ─────────────────────────────────────────────────────────────────────────────

diffusers==0.30.3        # 0.27.x incompatible with huggingface-hub>=0.25
controlnet-aux           # let pip pick latest pre-built wheel
torchmetrics[image]      # for LPIPS evaluation
open_clip_torch          # CLIPScore evaluation
pyyaml                   # config loading
gradio                   # demo UI
compel                   # long-prompt (>77 token) support for SD 1.5; optional fallback if absent

# ── DO NOT ADD BELOW (Colab provides these — reinstalling breaks CUDA) ────────
# torch
# torchvision
# accelerate
# peft
# transformers
# numpy
# scipy
# pandas
