# ── Core ML stack ────────────────────────────────────────────────────────────
# Colab already provides torch + torchvision; these are no-ops there.
torch>=2.1.0
torchvision>=0.16.0

# ── Diffusers ecosystem ───────────────────────────────────────────────────────
# Let pip resolve compatible versions across diffusers/transformers/accelerate/peft.
# Colab's pre-installed peft requires accelerate>=0.34 via clear_device_cache.
diffusers==0.30.3
transformers>=4.40.0
accelerate>=0.34.0
safetensors>=0.4.3
peft>=0.10.0

# ── ControlNet & attention ────────────────────────────────────────────────────
xformers>=0.0.25          # pip picks latest pre-built wheel; no source build
controlnet-aux>=0.0.7     # pip picks latest pre-built wheel; no source build

# ── Demo & evaluation ─────────────────────────────────────────────────────────
gradio>=4.29.0
torchmetrics[image]>=1.3.2
open_clip_torch>=2.24.0

# ── Utilities (loose pins — Colab 2024+ ships numpy 2.x) ─────────────────────
pyyaml>=6.0.1
pillow>=10.3.0
tqdm>=4.66.2
matplotlib>=3.8.4
pandas>=2.2.2
numpy>=1.26.4
